# Workaround: npm 10.9 (arborist) falla con "Cannot read properties of null (reading edgesOut)"
# al resolver el árbol de peers de este proyecto. No hay peers inválidos (npm ls limpio).
legacy-peer-deps=true
